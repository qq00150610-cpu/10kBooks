# 部署运维文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 版本 | v1.0.0 |
| 更新日期 | 2024-01-15 |
| 文档作者 | 运维团队 |

---

## 1. 环境要求

### 1.1 服务器配置要求

#### 开发环境

| 组件 | 配置 | 数量 |
|------|------|------|
| CPU | 4 核 | 1 |
| 内存 | 8 GB | 1 |
| 系统盘 | 100 GB SSD | 1 |
| 数据盘 | 200 GB SSD | 1 |

#### 测试环境

| 组件 | 配置 | 数量 |
|------|------|------|
| API 服务器 | 4 核 8GB | 2 |
| 数据库 | 8 核 16GB | 1 |
| Redis | 4 核 8GB | 1 |
| Elasticsearch | 4 核 8GB | 1 |

#### 生产环境

| 组件 | 配置 | 数量 | 说明 |
|------|------|------|------|
| API 服务器 | 8 核 16GB | 10+ | 弹性伸缩 |
| 数据库 | 16 核 64GB | 3 | 主从部署 |
| Redis Cluster | 8 核 32GB | 6 | 3 主 3 从 |
| Elasticsearch | 8 核 32GB | 5 | 3 主 2 从 |
| Kafka | 8 核 32GB | 6 | 3 节点集群 |
| 对象存储 | - | - | OSS/S3 |

### 1.2 软件版本要求

| 软件 | 版本 | 说明 |
|------|------|------|
| **操作系统** | | |
| CentOS | 7.9+ | 推荐 |
| Ubuntu | 20.04 LTS | 推荐 |
| **容器** | | |
| Docker | 24.0+ | 容器运行时 |
| Docker Compose | 2.20+ | 本地编排 |
| Kubernetes | 1.27+ | 生产编排 |
| **数据库** | | |
| MySQL | 8.0.35+ | 主数据库 |
| Redis | 7.0+ | 缓存 |
| MongoDB | 6.0+ | 文档存储 |
| Elasticsearch | 8.11+ | 搜索引擎 |
| **中间件** | | |
| Kafka | 3.6+ | 消息队列 |
| Nacos | 2.2+ | 注册配置 |
| Sentinel | 1.8+ | 限流熔断 |
| **开发工具** | | |
| Java | 17 LTS | 后端运行时 |
| Node.js | 18 LTS | 前端构建 |
| Python | 3.11+ | 脚本工具 |

### 1.3 网络要求

| 项目 | 要求 |
|------|------|
| **带宽** | 最低 10Mbps，推荐 100Mbps |
| **延迟** | 集群内网络延迟 < 1ms |
| **端口** | 开放必要端口，防火墙配置 |
| **CDN** | 推荐使用全球 CDN 加速 |

---

## 2. 部署步骤

### 2.1 基础环境准备

#### 2.1.1 系统配置

```bash
# 关闭 SELinux
setenforce 0
sed -i 's/SELINUX=enforcing/SELINUX=disabled/g' /etc/selinux/config

# 关闭防火墙 (测试环境)
systemctl stop firewalld
systemctl disable firewalld

# 安装依赖
yum install -y yum-utils device-mapper-persistent-data lvm2

# 添加 Docker 镜像源
yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# 安装 Docker
yum install -y docker-ce docker-ce-cli containerd.io

# 启动 Docker
systemctl start docker
systemctl enable docker
```

#### 2.1.2 安装 Docker Compose

```bash
# 下载 Docker Compose
curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# 添加执行权限
chmod +x /usr/local/bin/docker-compose

# 验证安装
docker-compose --version
```

#### 2.1.3 安装 Kubernetes

```bash
# 安装 kubeadm
cat <<EOF > /etc/yum.repos.d/kubernetes.repo
[kubernetes]
name=Kubernetes
baseurl=https://packages.cloud.google.com/yum/repos/kubernetes-el7-x86_64
enabled=1
gpgcheck=1
repo_gpgcheck=1
gpgkey=https://packages.cloud.google.com/yum/doc/yum-key.gpg
EOF

# 安装
yum install -y kubelet kubeadm kubectl

# 启动
systemctl enable kubelet
systemctl start kubelet
```

### 2.2 Docker Compose 部署 (开发/测试环境)

#### 2.2.1 创建项目目录

```bash
mkdir -p /opt/10kbooks
cd /opt/10kbooks

# 创建配置目录
mkdir -p config/nginx config/mysql config/redis
mkdir -p data/mysql data/redis data/elasticsearch
mkdir -p logs/nginx logs/mysql
```

#### 2.2.2 创建 docker-compose.yml

```yaml
version: '3.8'

services:
  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: 10kbooks-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: 10kbooks
      MYSQL_USER: 10kbooks
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - ./data/mysql:/var/lib/mysql
      - ./config/mysql:/etc/mysql/conf.d
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    command: --default-authentication-plugin=mysql_native_password
    networks:
      - 10kbooks-net

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: 10kbooks-redis
    restart: always
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - ./data/redis:/data
      - ./config/redis/redis.conf:/usr/local/etc/redis/redis.conf
    networks:
      - 10kbooks-net

  # Elasticsearch
  elasticsearch:
    image: elasticsearch:8.11.0
    container_name: 10kbooks-elasticsearch
    restart: always
    environment:
      - discovery.type=single-node
      - ES_JAVA_OPTS=-Xms2g -Xmx2g
      - xpack.security.enabled=false
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - ./data/elasticsearch:/usr/share/elasticsearch/data
    networks:
      - 10kbooks-net

  # Kafka
  kafka:
    image: bitnami/kafka:3.6
    container_name: 10kbooks-kafka
    restart: always
    environment:
      - KAFKA_CFG_NODE_ID=0
      - KAFKA_CFG_PROCESS_ROLES=controller,broker
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://localhost:9092
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=0@kafka:9093
      - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE=true
    ports:
      - "9092:9092"
    volumes:
      - ./data/kafka:/bitnami/kafka
    networks:
      - 10kbooks-net

  # Nacos 注册中心
  nacos:
    image: nacos/nacos-server:v2.2.3
    container_name: 10kbooks-nacos
    restart: always
    environment:
      - MODE=standalone
      - SPRING_DATASOURCE_PLATFORM=mysql
      - MYSQL_SERVICE_HOST=mysql
      - MYSQL_SERVICE_DB_NAME=nacos
      - MYSQL_SERVICE_USER=nacos
      - MYSQL_SERVICE_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_SERVICE_DB_PARAM=characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&useSSL=false
    ports:
      - "8848:8848"
      - "9848:9848"
    depends_on:
      - mysql
    networks:
      - 10kbooks-net

  # 后端服务
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: 10kbooks-backend
    restart: always
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - NACOS_SERVER_ADDR=nacos:8848
      - MYSQL_HOST=mysql
      - REDIS_HOST=redis
    ports:
      - "8080:8080"
    depends_on:
      - mysql
      - redis
      - nacos
    networks:
      - 10kbooks-net

  # Nginx 网关
  nginx:
    image: nginx:alpine
    container_name: 10kbooks-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./config/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./config/nginx/conf.d:/etc/nginx/conf.d
      - ./logs/nginx:/var/log/nginx
      - ../frontend/dist:/usr/share/nginx/html
    depends_on:
      - backend
    networks:
      - 10kbooks-net

networks:
  10kbooks-net:
    driver: bridge
```

#### 2.2.3 创建环境变量文件

```bash
# .env 文件
cat > .env << EOF
# MySQL 配置
MYSQL_ROOT_PASSWORD=RootPassword123!
MYSQL_PASSWORD=10kbooksPassword123!

# 应用配置
APP_ENV=production
JWT_SECRET=your-secret-key-here
EOF
```

#### 2.2.4 启动服务

```bash
# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f backend

# 停止服务
docker-compose down
```

### 2.3 Kubernetes 部署 (生产环境)

#### 2.3.1 Kubernetes 集群初始化

```bash
# 初始化主节点
kubeadm init --pod-network-cidr=10.244.0.0/16 --service-cidr=10.96.0.0/12

# 配置 kubectl
mkdir -p $HOME/.kube
cp -i /etc/kubernetes/admin.conf $HOME/.kube/config

# 安装网络插件 (Calico)
kubectl apply -f https://docs.projectcalico.org/manifests/calico.yaml

# 查看节点状态
kubectl get nodes
```

#### 2.3.2 创建 Namespace

```yaml
# namespace.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: 10kbooks
  labels:
    name: 10kbooks
```

#### 2.3.3 部署配置文件

```yaml
# configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: 10kbooks-config
  namespace: 10kbooks
data:
  application.yml: |
    spring:
      datasource:
        url: jdbc:mysql://mysql.10kbooks:3306/10kbooks?useUnicode=true&characterEncoding=utf8
        username: ${MYSQL_USER}
        password: ${MYSQL_PASSWORD}
      redis:
        host: redis.10kbooks
        port: 6379
    nacos:
      server-addr: nacos.10kbooks:8848
```

#### 2.3.4 部署 MySQL

```yaml
# mysql-deployment.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql
  namespace: 10kbooks
spec:
  serviceName: mysql
  replicas: 3
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: root-password
        volumeMounts:
        - name: mysql-data
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: mysql-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 100Gi
```

#### 2.3.5 部署后端服务

```yaml
# backend-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: 10kbooks
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: 10kbooks/backend:latest
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "2Gi"
            cpu: "1000m"
          limits:
            memory: "4Gi"
            cpu: "2000m"
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: 10kbooks
spec:
  selector:
    app: backend
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
```

#### 2.3.6 部署 Ingress

```yaml
# ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: 10kbooks-ingress
  namespace: 10kbooks
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
  - host: api.10kbooks.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 80
  tls:
  - hosts:
    - api.10kbooks.com
    secretName: tls-secret
```

#### 2.3.7 部署 HPA 自动扩缩容

```yaml
# hpa.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: backend-hpa
  namespace: 10kbooks
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: backend
  minReplicas: 3
  maxReplicas: 20
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
```

### 2.4 部署命令汇总

```bash
# 1. 克隆代码
git clone https://github.com/10kbooks/10kbooks.git
cd 10kbooks

# 2. 准备环境
mkdir -p /opt/10kbooks
cp -r infra/* /opt/10kbooks/

# 3. 配置环境变量
cd /opt/10kbooks
cp .env.example .env
vim .env  # 修改密码

# 4. 初始化数据库
mysql -u root -p < init.sql

# 5. 启动基础设施
docker-compose up -d mysql redis elasticsearch kafka nacos

# 6. 等待服务就绪
sleep 30
docker-compose ps

# 7. 构建后端
cd ../backend
mvn clean package -DskipTests
docker build -t 10kbooks/backend:latest .

# 8. 启动后端服务
docker-compose up -d backend

# 9. 构建前端
cd ../frontend
npm install
npm run build

# 10. 启动 Nginx
docker-compose up -d nginx

# 11. 验证部署
curl http://localhost/health
```

---

## 3. 配置说明

### 3.1 环境变量

| 变量名 | 说明 | 示例 |
|--------|------|------|
| SPRING_PROFILES_ACTIVE | Spring 环境 | dev / test / prod |
| MYSQL_HOST | MySQL 主机 | mysql |
| MYSQL_PORT | MySQL 端口 | 3306 |
| MYSQL_DATABASE | 数据库名 | 10kbooks |
| MYSQL_USER | 数据库用户 | 10kbooks |
| MYSQL_PASSWORD | 数据库密码 | ******* |
| REDIS_HOST | Redis 主机 | redis |
| REDIS_PORT | Redis 端口 | 6379 |
| NACOS_SERVER_ADDR | Nacos 地址 | nacos:8848 |
| JWT_SECRET | JWT 密钥 | ******* |
| OSS_ENDPOINT | OSS 端点 | oss-cn-beijing.aliyuncs.com |
| OSS_ACCESS_KEY | OSS 密钥 | ******* |
| OSS_BUCKET | OSS 桶名 | 10kbooks |

### 3.2 Nginx 配置

```nginx
# /etc/nginx/nginx.conf

user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;

events {
    worker_connections 10240;
    use epoll;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # 日志格式
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    # 性能优化
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 100m;

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    upstream backend {
        server backend:8080;
        keepalive 32;
    }

    server {
        listen 80;
        server_name _;

        # 前端静态文件
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        # API 代理
        location /api/ {
            proxy_pass http://backend/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # 静态资源
        location /assets/ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
}
```

### 3.3 MySQL 配置

```ini
# /etc/mysql/conf.d/custom.cnf

[mysqld]
# 基础设置
default-storage-engine=InnoDB
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci

# 连接设置
max_connections=1000
max_connect_errors=1000

# 缓存设置
innodb_buffer_pool_size=4G
query_cache_type=0  # MySQL 8.0 已移除

# 日志设置
slow_query_log=1
slow_query_log_file=/var/log/mysql/slow.log
long_query_time=2

# 安全设置
sql_mode=STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION
```

---

## 4. 常见问题

### 4.1 服务启动问题

#### 问题：MySQL 启动失败

```bash
# 查看日志
docker-compose logs mysql

# 解决方案：检查数据目录权限
chown -R 999:999 data/mysql

# 重新初始化
rm -rf data/mysql/*
docker-compose up -d mysql
```

#### 问题：端口冲突

```bash
# 检查端口占用
netstat -tlnp | grep 3306

# 修改端口或停止冲突服务
```

#### 问题：容器无法启动

```bash
# 查看详细日志
docker-compose logs -f <service-name>

# 检查资源
docker stats

# 重启服务
docker-compose restart <service-name>
```

### 4.2 性能问题

#### 问题：API 响应慢

```bash
# 检查后端日志
docker-compose logs backend | grep -i error

# 检查数据库连接
docker exec -it 10kbooks-mysql mysql -u root -p -e "SHOW PROCESSLIST;"

# 检查 Redis
docker exec -it 10kbooks-redis redis-cli INFO stats
```

#### 问题：内存不足

```bash
# 查看内存使用
free -h
docker stats --no-stream

# 优化 JVM 配置
# 在 docker-compose.yml 中添加:
# environment:
#   - JAVA_OPTS=-Xmx2g -Xms2g
```

### 4.3 网络问题

#### 问题：容器间无法通信

```bash
# 检查网络
docker network inspect 10kbooks_10kbooks-net

# 重建网络
docker-compose down
docker network prune
docker-compose up -d
```

---

## 5. 运维手册

### 5.1 日常检查

```bash
#!/bin/bash
# daily_check.sh - 每日巡检脚本

echo "========== 10kBooks 日常巡检 =========="
echo "时间: $(date)"
echo

# 1. 服务状态检查
echo ">>> 服务状态"
docker-compose ps

# 2. 资源使用
echo
echo ">>> 资源使用"
docker stats --no-stream

# 3. 磁盘使用
echo
echo ">>> 磁盘使用"
df -h

# 4. 日志错误统计
echo
echo ">>> 最近 1 小时错误日志"
docker-compose logs --since=1h | grep -i error | tail -20

# 5. 数据库连接
echo
echo ">>> 数据库连接数"
docker exec -it 10kbooks-mysql mysql -u root -p${MYSQL_PASSWORD} -e "SELECT COUNT(*) as 'Active Connections' FROM information_schema.processlist WHERE command != 'Binlog Dump';"

# 6. Redis 状态
echo
echo ">>> Redis 状态"
docker exec -it 10kbooks-redis redis-cli INFO | grep -E "used_memory_human|connected_clients"
```

### 5.2 日志管理

```bash
# 查看后端日志
docker-compose logs -f backend

# 查看 Nginx 访问日志
docker exec -it 10kbooks-nginx tail -f /var/log/nginx/access.log

# 日志归档
find logs/ -name "*.log" -mtime +7 -exec gzip {} \;
find logs/ -name "*.gz" -mtime +30 -delete
```

### 5.3 备份恢复

#### 自动备份脚本

```bash
#!/bin/bash
# backup.sh - 数据库备份脚本

BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d)
MYSQL_CONTAINER="10kbooks-mysql"

# 创建备份目录
mkdir -p ${BACKUP_DIR}

# 备份数据库
docker exec ${MYSQL_CONTAINER} mysqldump -u root -p${MYSQL_PASSWORD} \
    --single-transaction \
    --routines \
    --triggers \
    --events \
    10kbooks > ${BACKUP_DIR}/10kbooks_${DATE}.sql

# 压缩备份
gzip ${BACKUP_DIR}/10kbooks_${DATE}.sql

# 上传到 OSS (可选)
# ossutil cp ${BACKUP_DIR}/10kbooks_${DATE}.sql.gz oss://10kbooks-backup/

# 删除 30 天前的备份
find ${BACKUP_DIR} -name "*.gz" -mtime +30 -delete

echo "Backup completed: ${DATE}"
```

#### 数据恢复

```bash
# 恢复数据库
gunzip < 10kbooks_20240115.sql.gz | docker exec -i 10kbooks-mysql mysql -u root -p${MYSQL_PASSWORD} 10kbooks
```

### 5.4 监控告警

#### Prometheus 指标

| 指标 | 说明 |
|------|------|
| http_server_requests_seconds | HTTP 请求延迟 |
| jvm_memory_used_bytes | JVM 内存使用 |
| mysql_connections | 数据库连接数 |
| redis_connected_clients | Redis 连接数 |
| kafka_consumer_lag | Kafka 消费延迟 |

#### 告警规则

```yaml
# alert-rules.yaml
groups:
- name: 10kbooks-alerts
  rules:
  - alert: ServiceDown
    expr: up{job="backend"} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: "Backend service is down"
  
  - alert: HighResponseTime
    expr: histogram_quantile(0.95, rate(http_server_requests_seconds_bucket[5m])) > 2
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: "High API response time"
```

---

## 附录

### A. 快速命令参考

| 命令 | 说明 |
|------|------|
| `docker-compose up -d` | 启动所有服务 |
| `docker-compose down` | 停止所有服务 |
| `docker-compose restart <service>` | 重启服务 |
| `docker-compose logs -f <service>` | 查看日志 |
| `docker-compose ps` | 查看状态 |
| `docker exec -it <container> sh` | 进入容器 |
| `kubectl get pods -n 10kbooks` | 查看 Pod |
| `kubectl logs -f <pod> -n 10kbooks` | 查看 K8s 日志 |

### B. 联系方式

- 运维团队：ops@10kbooks.com
- 紧急联系：+86 xxx-xxxx-xxxx
- 值班钉钉群：10kbooks-oncall
